Note : Admin Sidebar should be same and include the following (for each page)
        Dashboard
        Products (Manage Products)
        Facilities
        Catalog Assignment
        Orders / Invoices
        Logout

Dashboard:
        - Add dropdown as selector in Revenue Trackinf instead of displaying at the top (this week/ this month/ this year/ last 5 years).
        - Invoice instead of invoice status in order history table

Manage Products:
        - The header is manage products, not product analytics.

Facilities:
        - Can an admin add a facility? (for now, we are assuming, that facilities can register themselves through a btn on the home/landing page of e-comm)
        - Facility can have just a single manager or more managers? (for now, let's assume that there is only one manager per facility- the person who is registering the facility)


Catalog Assignment:
        - There should be a better way to display price bucket and max price limit